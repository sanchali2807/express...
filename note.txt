express is a minimal node js web framework 
both service and building API


express .static is an express middleware to server static files directly to the server 
in exress for you to use env file you need to explicitly define the env file in package.json
so you need to write --env-file=.env in dev or start 
and need to make changes to package.json  like dev for npm run dev or nodemon in start


200 success
300 redirect
400 client error
500 server error


so we must use require with module.export this is common js language

and import export is es module language 

Router is a function predefined in express used to create modular routes , acts like a mini express handler that is then attched to main express using app.use